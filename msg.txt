🧹 [code health] Refactor console.error to console.warn in ChannelGrid

🎯 What: Replaced a generic `console.error` with a descriptive `console.warn` when saving to localStorage fails in `ChannelGrid.tsx`.
💡 Why: Improves maintainability by providing context to the warning and reduces unnecessary error noise since localStorage failures are often expected in restrictive environments (e.g., incognito mode).
✅ Verification: Ran `npm run lint` and `npm run build`, verified the code change, reverted unintended `package-lock.json` modifications, and requested a code review which passed.
✨ Result: Cleaner console output and a more maintainable error handling pattern matching the surrounding code.
